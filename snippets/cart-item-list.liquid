<ul class="cartitems--list" data-cart-item-list data-ajax-cart-section>
  {% for item in cart.items %}
    {%
      render 'cart-item',
      item: item,
      show_sku: show_sku,
      item_index: forloop.index,
    %}
  {% endfor %}
</ul>
